{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CodebuffEvent",
  "description": "Codebuff CLI streaming event types (PrintModeEvent). Events are parsed dynamically from JSON.",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "description": "Event type: start, error, download, tool_call, tool_result, tool_progress, text, finish, subagent_start, subagent_finish, reasoning_delta"
    }
  },
  "required": ["type"],
  "additionalProperties": true
}
